# 2026-07-08 — 015: none-vs-sbx comparison figures

**Task:** Manuel: small experiment based on 014 comparing voltage
tracking quality, tie-line flows, and the reactive infeed of
generators, DER and DSOs between the `none` and `sbx` arms, with good
visualisations.

**Changed:** `experiments/015_SBX_COMPARE.py` (new). Reads the stored
013 campaign pickles (360 min, calibrated band) — nothing re-runs by
default; `--run` executes missing arms through the 013 machinery.
Figures per scenario under `results/015_SBX_COMPARE/<scenario>/`:

- `F1_voltage_tracking.png` — per zone: recorded voltage-tracking RMS
  error (`zone_v_rms_err_pu`) and the [min, max] voltage envelope with
  the zone's hard bounds; stress window shaded, freeze tick dotted.
- `F2_corridor_flows.png` — per corridor: `zone_tie_q_mvar` of both
  arms overlaid + the sbx schedule staircase and deal markers for
  context (F7 convention note in the module docstring: identical in
  both arms, so the comparison is unaffected).
- `F3_reactive_infeed.png` — 3 zones × 3 sources (sync generators,
  TSO-DER, DSO interface Q) both arms overlaid; "not recorded"
  placeholder where a zone hosts no source of that kind (zone-1 DSO).
- `SUMMARY.md` — mean tracking RMS per zone and arm.

Fixed arm colours across every panel (none = grey #888888, sbx = blue
#4477aa; CVD-safe pair); one axis per panel; recessive grids.

**First result (asym_z3):** mean tracking RMS — z1 5.93→6.01 mpu,
z2 7.02→7.10 mpu (supporters pay a marginal tracking cost for the
acting-side dv), z3 12.84→12.78 mpu (the requester gains). F3 shows the
mechanism's footprint by source: zone-1 DER exports visibly more Q
under sbx during and after the stress; zone-3 sources are relieved.

**Status:** done; runs in seconds from stored pickles on any of the
five 013 scenarios.
